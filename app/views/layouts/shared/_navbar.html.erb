<!--*********************   navbar  **********************-->
<nav class="navbar navbar-expand-lg navbar-transparent bg-primary navbar-absolute" >
  <div class="container" ng-controller="navbarController">
    <div class="navbar-translate">
      <button class="navbar-toggler" type="button" data-toggle="collapse" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-bar bar1"></span>
        <span class="navbar-toggler-bar bar2"></span>
        <span class="navbar-toggler-bar bar3"></span>
      </button>
        <a class="navbar-brand" href="">Nguyen Chi Max</a>
    </div>
    
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="#" ng-click="redirectHome()">Home</a>
        </li>
        <li class="nav-item dropdown" ng-repeat="x in categories">
          <a href="#" class="nav-link dropdown-toggle" id="navbarDropdownMenuLink{{x.id}}" data-toggle="dropdown" value="{{x.id}}" ng-click="myCategory(x.id)">
			      <p>{{x.name}}</p>
		      </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink{{x.id}}">
            <a class="dropdown-item" ng-repeat="y in categoriesId" href="#{{y.id}}" ng-click="clickLink(y.id)">
              <i class="fa fa-cubes" aria-hidden="true"></i>
                {{y.name}}
            </a>
          </div>
        </li>
       
        <li class="nav-item dropdown">
         <% if logged_in? %>
					<a href="#" class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown">
						<span class="badge badge-primary">{{carts.length}}</span><p id="user" data-value="<%=current_user.id%>"><%=current_user.name%></p>
					</a>
					<div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
						<a class="dropdown-item" href="/signup">
              <i class="now-ui-icons users_circle-08"></i>
            	Đăng kí
						</a>
            <a class="dropdown-item" href="#" ng-click="redirect(<%=current_user.id%>)">
              <i class="now-ui-icons users_circle-08"></i>
                Giỏ hàng <span class="badge badge-primary" id="total" data-value="<%=current_user.id%>">{{carts.length}}</span>
						</a>
						<a class="dropdown-item" href="/logout">
              <i class="fa fa-battery-three-quarters" aria-hidden="true"></i>
							Đăng xuất
						</a>
					</div>
          <%else%>
          <li class="nav-item">
            <a class="nav-link" href="/login"id="user" data-value="">
              Đăng nhập
            </a>
          </li>
          <% end %>
				</li>
      </ul>
    </div>
  </div>
</nav>
<!--*********************   End navbar  **********************-->